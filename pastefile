$blueZone = New-Object -ComObject BZWhll.WhllObj
$result = $blueZone.Connect("A") # Replace "A" with your session name
if ($result -eq 0) {
    Write-Output "Connected successfully"
} else {
    Write-Output "Failed to connect. Error code: $result"
}
