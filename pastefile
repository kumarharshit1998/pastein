
<style>
    .form-container {
        display: flex;
        flex-wrap: wrap;
        gap: 15px;
        align-items: center;
        justify-content: space-between;
        max-width: 100%;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        min-width: 200px;
    }

    select, input {
        padding: 8px;
        width: 100%;
        box-sizing: border-box;
    }

    button {
        padding: 10px 20px;
        background-color: #2E1A47;
        color: white;
        border: none;
        cursor: pointer;
        border-radius: 5px;
    }
</style>
