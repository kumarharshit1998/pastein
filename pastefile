<style>
    .form-container {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        justify-content: space-between;
        max-width: 90%; /* Reduces overall width */
        margin: auto;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        flex: 1 1 calc(25% - 10px); /* Makes 4 columns per row */
        min-width: 150px; /* Prevents fields from shrinking too much */
    }

    label {
        font-size: 14px;
        font-weight: bold;
    }

    select, input {
        padding: 6px;
        font-size: 14px;
        width: 100%;
        box-sizing: border-box;
    }

    button {
        padding: 8px 15px;
        background-color: #2E1A47;
        color: white;
        border: none;
        cursor: pointer;
        border-radius: 5px;
    }
</style>
